Phase 4 — Layout Shell
4.1  MainLayout: CSS grid — sidebar (280px) + main area
4.2  Header: logo left, user avatar + sign-out right
4.3  Sidebar: static shell (no data) — upload area + list area placeholders
4.4  DashboardPage: renders MainLayout with placeholders

verify: layout looks correct on desktop
        sidebar collapses on mobile (md: breakpoint)

Phase 5 — Upload Flow
5.1  api/documents.js: uploadDocument(file, userId) → POST /upload
5.2  useUpload.js: TanStack mutation wrapping uploadDocument
5.3  UploadZone: drag-drop + click-to-browse, file type/size validation
5.4  Connect useUpload to UploadZone
5.5  Sonner toasts: success / error / in-progress
5.6  Add uploaded doc to Zustand store on success

verify: upload a PDF → toast shows → backend ingests it
        wrong file type → rejected before API call
        oversized file → rejected before API call

Phase 6 — Document Management
6.1  Zustand documents slice: addDocument, removeDocument, toggleSelect
6.2  DocumentItem: filename, type icon, chunk count, status chip, checkbox
6.3  DocumentList: renders DocumentItem list from store
6.4  DocumentDetail: hover shows summary + key_topics in a popover
6.5  Selection state: checked docs = scope for next query

verify: upload 2 docs → both appear in sidebar
        hover shows summary
        checkboxes work independently

Phase 7 — Query + Chat
7.1  api/query.js: sendQuery(text, userId, docIds) → POST /query
7.2  useQuery.js: TanStack mutation, appends messages to Zustand on settle
7.3  QueryInput: textarea, Enter to send, Shift+Enter newline, disabled while loading
7.4  MessageBubble: user (right-aligned) + assistant (left-aligned) variants
7.5  ChatWindow: message list, auto-scroll to bottom on new message
7.6  Wire QueryInput → useQuery → ChatWindow

verify: type question → answer appears → chat scrolls
        loading state visible while waiting
        second question works

Phase 8 — AI Response Details
8.1  CitationCard: excerpt + page number + doc name
8.2  CitationList: collapsible "N sources" toggle under AI message
8.3  IntentBadge: small chip showing factual / summarize / explain / compare
8.4  Empty state: no documents uploaded yet (with upload prompt)
8.5  Empty state: no messages yet (with suggested questions)
8.6  Suggested questions: 3 clickable chips that pre-fill QueryInput

verify: AI answer shows citation count
        expanding citations shows page + excerpt
        intent badge matches query type

Phase 9 — Error + Loading States
9.1  Spinner: reusable loading indicator used in UploadZone + ChatWindow
9.2  Skeleton: message placeholder while query is running
9.3  ErrorBoundary: wraps ChatWindow + Sidebar independently
9.4  Upload error: show inline error below UploadZone (not just toast)
9.5  Query error: show error message in chat as system message
9.6  Network offline: detect + show banner

verify: disconnect backend → graceful error, not blank screen
        reload with no docs → empty state shows

Phase 10 — Deploy + Harden
10.1  .env.production: API URL points to deployed backend
10.2  Vercel: deploy frontend (connect GitHub repo)
10.3  Railway/Render: deploy FastAPI backend
10.4  FastAPI Clerk middleware: verify JWT on backend (optional but impressive)
10.5  Sentry: add to frontend + backend for error tracking
10.6  README: architecture diagram + live demo link + stack badges
10.7  Performance: React.lazy for DashboardPage, code splitting
10.8  Final check: Lighthouse score, mobile layout

verify: live URL works
        login on Vercel → upload → query → working